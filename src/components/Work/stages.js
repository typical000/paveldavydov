// When list is statically displayed
const LIST = 'LIST'

// When list is animated into project
const LIST_TO_PROJECT = 'LIST_TO_PROJECT'

// Switching between slides inside slider
const SWITCHING = 'SWITCHING'

// Statically displayed project
const PROJECT = 'PROJECT'

// Animation from project into list
const PROJECT_TO_LIST = 'PROJECT_TO_LIST'

export default {
  LIST,
  LIST_TO_PROJECT,
  SWITCHING,
  PROJECT,
  PROJECT_TO_LIST,
}
